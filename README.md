# Hister Config

My [Hister](https://hister.org) configuration and an install script. This sets
up a `launchd` agent to make Hister easy to control.

## Usage

Just run `./install.sh` to install. After that, Hister will be running. The
script requires `yq` to be installed.

